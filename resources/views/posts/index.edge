@layout('main')

@section('content')
  <h1 class="alert alert-primary mt-2">{{ title }}</h1>
  @if(old('notification'))
    <div class="alert alert-success">
      {{ old('notification') }}
    </div> </br>
  @endif
  <ul class="list-group">
  @each(post in posts)
    <li class="list-group-item mb-2"><a href="/posts/{{ post.id }}">ini adalah :{{ post.title }}</a></li>
    <li class="list-group-item mb-4">deskripsi : {{ post.body }}</li>
  @endeach
  </ul>
@endsection
